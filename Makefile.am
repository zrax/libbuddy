EXTRA_DIST = autogen.bash

CLEANFILES = *~

MAINTAINERCLEANFILES = Makefile.in configure config/* aclocal.m4 \
					   config.h config.h.in INSTALL COPYING libtool
EXTRA_DIST = doxygen.cfg

SUBDIRS = src pkg-config
